spring:
  datasource:
    url: jdbc:h2:mem:vecoassets;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

app:
  cors:
    allowed-origins: http://localhost:5173
